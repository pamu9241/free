// JavaScript code goes here

        // Initialize constiables
        let currentBookingCost = 0;
        let overallBookingCost = 0;
        let loyaltyPoints = 0;

        // Function to calculate room booking cost
        function calculateRoomBookingCost() {
            // Add your room booking calculations here
        }

        // Function to calculate adventure booking cost
        function calculateAdventureBookingCost() {
            // Add your adventure booking calculations here
        }

        // Function to update UI with current booking details
        function updateCurrentBooking() {
            // Add your code to update the current booking details in the UI
        }

        // Function to update UI with overall booking details
        function updateOverallBooking() {
            // Add your code to update the overall booking details in the UI
        }

        // Function to save order as a favorite in local storage
        function addToFavorites() {
            // Add your code to save the current booking as a favorite in local storage
        }

        // Function to check loyalty points and display to the user
        function checkLoyaltyPoints() {
            // Add your code to check and display loyalty points
        }

        // Function to handle the "Book Now" button click
        function bookNow() {
            // Add your code to update overall booking, reset current booking, and update loyalty points
        }

        // Function to get values from the form and perform calculations
        function getValues() {
            // Add your code to get values from the form and perform calculations
        }

        // Add event listeners to call functions on appropriate events
        document.getElementById('submit').addEventListener('click', bookNow);
        document.getElementById('addToFavorites').addEventListener('click', addToFavorites);
        document.getElementById('checkLoyalty').addEventListener('click', checkLoyaltyPoints);